# 安装说明：
# 1. 先安装PyTorch（CPU版本）
#    pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu
#    pip install torchvision==0.16.0+cpu --index-url https://download.pytorch.org/whl/cpu
# 2. 按顺序安装sentence-transformers依赖
#    pip install tokenizers==0.13.2
#    pip install huggingface-hub==0.11.1
#    pip install transformers==4.21.0
#    pip install sentence-transformers==2.2.2
# 3. 然后安装其他依赖
#    pip install -r requirements.txt

praw==7.8.1
sqlalchemy==2.0.23
requests==2.31.0
python-dotenv==1.0.0
pandas==2.1.4
numpy==1.24.3
openpyxl==3.1.2
openai==1.3.7
anthropic==0.7.8
aiohttp==3.9.1
schedule==1.2.0
scikit-learn==1.3.2
streamlit==1.28.2

# sentence-transformers相关依赖（需按上述顺序安装）
transformers==4.21.0
tokenizers==0.13.2  # 注意：anthropic需要>=0.13.0
huggingface-hub==0.11.1
sentence-transformers==2.2.2
